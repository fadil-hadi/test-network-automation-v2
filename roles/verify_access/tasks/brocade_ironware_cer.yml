---
- name: "[Brocade CER] Verify SSH access and capture version"
  ansible.builtin.expect:
    command: >-
      ssh {{ legacy_ssh_crypto_args }} {{ ansible_user }}@{{ ansible_host }}
    responses:
      "[Pp]assword:\\s*$":
        - "{{ ansible_password }}"
      "[>#]\\s*$":
        - "show version"
        - "exit"
    timeout: 20
  delegate_to: localhost
  changed_when: false
  register: verify_access_result

- name: "Record access check result"
  ansible.builtin.set_fact:
    verify_access_status: "{{ 'PASS' if verify_access_result.rc == 0 else 'FAIL' }}"
    verify_access_detail: "SSH login + show version succeeded"

