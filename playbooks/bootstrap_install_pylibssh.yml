---
- hosts: localhost
  gather_facts: false
  tasks:
    - name: Install pexpect (pure Python, no compiler needed)
      ansible.builtin.pip:
        name: pexpect
        extra_args: --break-system-packages
