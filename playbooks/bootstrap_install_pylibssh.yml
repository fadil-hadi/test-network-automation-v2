---
- hosts: localhost
  gather_facts: false
  tasks:
    - name: Install ansible-pylibssh for legacy SSH device support
      ansible.builtin.pip:
        name: ansible-pylibssh
        extra_args: --break-system-packages
