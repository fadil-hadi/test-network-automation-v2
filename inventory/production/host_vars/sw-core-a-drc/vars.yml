---
# host_vars for: sw-core-a-drc
# Hardware : S8700-4
# Role     : huawei_dc_core
# Notes    : Switch - Core A DRC
# ─────────────────────────────────────────────────────────

# FIRMWARE OVERRIDE — this unit runs VRP V600 (V600R022/V600R024), not the
# V200 branch most of this group is on. Overriding device_series so it uses
# a separate task file per role until validated identical to V200 behavior.
# See docs/firmware-notes.md.
device_series: "huawei_vrp_s_v600"

# Device-specific variables for this Huawei switch.
# Add IP interfaces, VLANIFs, loopbacks specific to this device only.
# Changes here affect ONLY this device when playbooks run.

# Example VLANIF interfaces — fill in actual values
# vlan_interfaces:
#   - vlan_id: 10
#     ip: "FILL_IP"
#     mask: "255.255.255.0"
#     description: "MGMT VLAN"

# Example loopback — fill in actual values
# loopback_interfaces:
#   - id: 0
#     ip: "FILL_IP"
#     mask: "255.255.255.255"
#     description: "Loopback — sw-core-a-drc"

# Example static route
# static_routes:
#   - destination: "0.0.0.0"
#     mask: "0.0.0.0"
#     nexthop: "FILL_GW_IP"
